defaultNamespace: spedge

# Netbird VPN package: deployed to spedge clusters labelled package-netbird=true
targets:
  - clusterSelector:
      matchLabels:
        fleet-env: spedge
        package-netbird: "true"
