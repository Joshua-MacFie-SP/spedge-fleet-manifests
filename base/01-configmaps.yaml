---
# Node-RED settings.js configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: nodered-settings
  namespace: spedge
  labels:
    app: nodered
data:
  settings.js: |
    module.exports = {
        adminAuth: {
            type: "credentials",
            users: [
                {
                    username: "admin",
                    // Default password hash - change in production (Phase 1b)
                    password: "$2y$08$u9mTJqNZF/xtrHqH7U3p4OqrSMa/LePyvjYd3GD4XLcrTEztuU0Ay",
                    permissions: "*"
                }
            ]
        },
        // Enable projects feature
        editorTheme: {
            projects: {
                enabled: true
            }
        },
        // Logging
        logging: {
            console: {
                level: "info",
                metrics: false,
                audit: false
            }
        }
    }

# Redis ACL is now generated by init container from secrets (see 03-redis.yaml)
